name: Deploy to GitHub Pages

on:
  push:
    branches: [ master ] # Або master, залежно від назви твоєї головної гілки

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repository
        uses: actions/checkout@v4

      - name: Install, build, and upload your site
        uses: withastro/action@v3
        # Якщо ти використовуєш pnpm або yarn замість npm, action підлаштується сам