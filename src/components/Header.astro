<header class="site-header">
  <div class="header-container">
    <a href="/" class="logo">Volya<span>Core</span></a>

    <div class="search-container">
      <div class="search-wrapper">
        <span class="search-icon">🔍</span>
        <input
          type="text"
          id="global-search"
          placeholder="Знайти улюблений проєкт..."
          autocomplete="off"
        />
      </div>
    </div>

    <div class="header-actions-wrapper">
      <div class="header-nav-wrapper" id="header-menu">
        <nav class="navigation">
          <a href="/">Каталог</a>
          <a href="/watchlist" class="nav-link">Мої списки</a>
          <a href="https://t.me/volyacore" target="_blank" class="tg-channel-btn">Телеграм</a>
        </nav>
      </div>

      <button
        id="theme-toggle"
        class="theme-toggle-btn"
        aria-label="Переключити тему"
      >
        <span class="sun-icon">☀️</span>
        <span class="moon-icon">🌙</span>
      </button>

      <button class="burger-menu-btn" id="burger-btn" aria-label="Відкрити меню">
        <span class="burger-line"></span>
        <span class="burger-line"></span>
        <span class="burger-line"></span>
      </button>
    </div>
  </div>
</header>

<script>
  function setupHeaderLogic() {
    const toggleBtn = document.getElementById("theme-toggle");
    toggleBtn?.addEventListener("click", () => {
      const currentTheme = document.documentElement.getAttribute("data-theme") || "dark";
      const newTheme = currentTheme === "light" ? "dark" : "light";
      document.documentElement.setAttribute("data-theme", newTheme);
      localStorage.setItem("theme", newTheme);
    });

    const burgerBtn = document.getElementById("burger-btn");
    const headerMenu = document.getElementById("header-menu");

    if (burgerBtn && headerMenu) {
      burgerBtn.classList.remove("active");
      headerMenu.classList.remove("open");
      document.body.classList.remove("lock-scroll");

      burgerBtn.addEventListener("click", (e) => {
        e.stopPropagation();
        burgerBtn.classList.toggle("active");
        headerMenu.classList.toggle("open");
        document.body.classList.toggle("lock-scroll");
      });

      const navLinks = headerMenu.querySelectorAll("a");
      navLinks.forEach(link => {
        link.addEventListener("click", () => {
          burgerBtn.classList.remove("active");
          headerMenu.classList.remove("open");
          document.body.classList.remove("lock-scroll");
        });
      });
    }
  }

  setupHeaderLogic();
  document.addEventListener("astro:after-swap", setupHeaderLogic);
</script>

<style lang="scss">
  .site-header {
    --bg-header: var(--bg-card);
    
    background: var(--bg-header) !important;
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border-color);
    position: sticky;
    top: 0;
    z-index: 100;
    transition: background 0.3s ease, border-color 0.3s ease;
  }

  :global([data-theme="light"]) .site-header { 
    --bg-header: #7C9BCC;
  }

  .header-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 15px 20px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 40px; /* Трохи зменшили стандартний зазор для гнучкості */
  }

  .logo {
    font-family: "Raleway", sans-serif;
    font-size: 24px;
    font-weight: 800;
    color: var(--text-primary);
    text-decoration: none;
    z-index: 102;
    flex-shrink: 0;
    
    span {
      color: #2563eb;
      text-shadow: 0 0 15px rgba(59, 130, 246, 0.4);
      transition: all 0.3s ease;
    }
  }

  /* 💎 НІЖНЕ ГОЛУБЕ СВІЧЕННЯ ЛОГОТИПУ ДЛЯ СВІТЛОЇ ТЕМИ */
  :global([data-theme="light"]) .logo {
    span {
      color: #ffffff !important;
      text-shadow: 
        0 0 10px rgba(255, 255, 255, 0.8),
        0 0 20px rgba(96, 165, 250, 0.6) !important; 
    }
  }

  /* Правий блок дій: об'єднує меню, перемикач темы та бургер */
  .header-actions-wrapper {
    display: flex;
    align-items: center;
    gap: 15px;
  }

  .header-nav-wrapper {
    display: flex;
    align-items: center;
  }

  .search-container {
    max-width: 450px;
    width: 100%;
    flex-grow: 1;
    z-index: 102; /* Щоб пошук не перекривався підкладками */
  }

  .search-wrapper {
    position: relative;
    display: flex;
    align-items: center;
    background: var(--bg-main);
    border: 2px solid #2563eb;
    border-radius: 50px;
    padding: 2px;
    transition: all 0.3s ease;
    box-shadow: 0 0 15px rgba(37, 99, 235, 0.1);

    .search-icon {
      position: absolute;
      left: 16px;
      font-size: 14px;
      color: var(--text-primary);
    }

    input {
      width: 100%;
      padding: 10px 16px 10px 44px;
      background: transparent;
      border: none;
      color: var(--text-primary) !important;
      font-size: 14px;
      outline: none;

      &::placeholder {
        color: var(--text-muted);
        opacity: 0.8;
      }
    }

    &:focus-within {
      border-color: #60a5fa;
      box-shadow: 0 0 25px rgba(37, 99, 235, 0.4);
    }
  }

  /* 💎 ТОЧНО ТАКЕ Ж ГОЛУБЕ СВІЧЕННЯ ДЛЯ ПОШУКУ ЯК І В ЛОГОТИПІ */
  :global([data-theme="light"]) .search-wrapper {
    border-color: #ffffff !important; /* Біла рамка в тон логотипу */
    box-shadow: 
      0 0 10px rgba(255, 255, 255, 0.8),
      0 0 20px rgba(96, 165, 250, 0.6) !important;

    &:focus-within {
      border-color: #ffffff !important;
      /* При фокусі робимо свічення ледь помітно щільнішим, зберігаючи ніжний тон */
      box-shadow: 
        0 0 12px rgba(255, 255, 255, 0.9),
        0 0 25px rgba(96, 165, 250, 0.8) !important;
    }
  }

  .theme-toggle-btn {
    background: var(--bg-button);
    border: 1px solid #2563eb;
    cursor: pointer;
    padding: 10px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    font-size: 16px;
    z-index: 102;
    flex-shrink: 0;

    &:hover {
      transform: scale(1.1);
      border-color: #2563eb;
    }
  }

  /* Додаткове вирівнювання та підсвітка кнопки зміни теми для білої теми */
  :global([data-theme="light"]) .theme-toggle-btn {
    border-color: #ffffff;
    box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
    &:hover {
      border-color: #e0f2fe;
      box-shadow: 0 0 15px rgba(255, 255, 255, 0.6);
    }
  }

  .navigation {
    display: flex;
    gap: 25px;
    align-items: center;
    flex-shrink: 0;

    a {
      color: var(--text-muted);
      text-decoration: none;
      font-weight: 600;
      transition: color 0.3s ease;
      &:hover {
        color: var(--text-primary);
      }
    }
    .tg-channel-btn {
      background: #2563eb;
      color: white !important;
      padding: 8px 16px;
      border-radius: 8px;
      &:hover {
        background: #1d4ed8;
      }
    }
  }

  /* Перефарбовування фону кнопки ТГ у світлій темі для кращого контрасту */
  :global([data-theme="light"]) .navigation .tg-channel-btn {
    background: #ffffff;
    color: #1e3a8a !important;
    font-weight: 700;
    box-shadow: 0 4px 12px rgba(255, 255, 255, 0.25);
    &:hover {
      background: #f8fafc;
      box-shadow: 0 4px 18px rgba(255, 255, 255, 0.45);
    }
  }

  .burger-menu-btn {
    display: none;
    flex-direction: column;
    justify-content: space-between;
    width: 24px;
    height: 18px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    z-index: 102;

    .burger-line {
      width: 100%;
      height: 2px;
      background-color: var(--text-primary);
      border-radius: 2px;
      transition: all 0.3s ease;
    }

    &.active {
      .burger-line:nth-child(1) { transform: translateY(8px) rotate(45deg); }
      .burger-line:nth-child(2) { opacity: 0; transform: translateX(-10px); }
      .burger-line:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }
    }
  }

  .sun-icon { display: none; }
  .moon-icon { display: inline; }

  :global([data-theme="light"]) {
    .sun-icon { display: inline; }
    .moon-icon { display: none; }
  }

  /* ==========================================================================
     📱 АДАПТИВНІ СТИЛІ (ПЕРЕМІЩЕННЯ ПОШУКУ НА МОБІЛЬНИХ)
     ========================================================================== */
  @media (max-width: 820px) {
    .header-container {
      padding: 12px 16px;
      gap: 15px; /* Стискаємо відстань між елементами шапки */
    }

    .burger-menu-btn {
      display: flex;
    }

    /* 🔍 Пошукова панель на мобільних стискається та стає ліворуч від кнопок дій */
    .search-container {
      max-width: 280px; 
      order: 0; /* Тримає позицію у центрі між лого та кнопками */
    }

    .search-wrapper input {
      padding: 8px 12px 8px 36px;
      font-size: 13px;
    }

    .search-wrapper .search-icon {
      left: 12px;
      font-size: 12px;
    }

    /* 🪐 Навігаційне меню (тепер ТІЛЬКИ для посилань каталогу та списків) */
    .header-nav-wrapper {
      position: fixed;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100vh;
      background: var(--bg-main); 
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 80px 24px 40px;
      transition: left 0.4s cubic-bezier(0.16, 1, 0.3, 1);
      z-index: 101;
      visibility: hidden;

      &.open {
        left: 0;
        visibility: visible;
      }
    }

    :global([data-theme="light"]) .header-nav-wrapper {
      background: #D9E6FF; 
    }

    .navigation {
      flex-direction: column;
      width: 100%;
      gap: 25px;

      a {
        font-size: 22px;
        font-weight: 700;
        width: 100%;
        text-align: center;
        padding: 8px 0;
        color: var(--text-primary) !important;
        
        &:hover {
          color: #2563eb !important;
        }
      }

      :global([data-theme="light"]) & a:hover {
        color: #ffffff !important;
      }

      .tg-channel-btn {
        width: 100%;
        max-width: 280px;
        padding: 14px;
        border-radius: 50px;
        color: white !important;
        margin-top: 15px;
      }
    }
  }

  /* Для зовсім маленьких екранів звужуємо поле пошуку, щоб усе влізло */
  @media (max-width: 480px) {
    .search-container {
      max-width: 160px; /* Зменшуємо ширину, щоб помістився логотип і кнопки */
    }
    .search-wrapper input::placeholder {
      color: transparent; /* Ховаємо плейсхолдер на крихітних екранах, залишаючи лише лупу */
    }
  }

  @media (max-width: 360px) {
    .logo {
      font-size: 18px;
    }
    .search-container {
      max-width: 110px;
    }
  }
</style>