---
import MainLayout from "../layouts/MainLayout.astro";
import MovieCard from "../components/MovieCard.astro";
import lakornsData from "../data/lakorns.json";

// Відображаємо перші 24 штуки для початкового стану
const latestLakorns = lakornsData.slice(0, 24);
---

<MainLayout title="VolyaCore">
  
  <section class="hero-carousel-section">
    <div class="carousel-container">
      <div class="carousel-track" id="carousel-track">
        {
          lakornsData.map((lakorn) => (
            <div class="carousel-slide-wrapper" data-id={lakorn.id}>
              <MovieCard
                id={lakorn.id}
                title={lakorn.title}
                enTitle={lakorn.enTitle}
                cover={lakorn.cover}
                status={lakorn.status}
                episodesCount={lakorn.episodesCount}
                country={lakorn.country}
                year={lakorn.year}
                genres={lakorn.genres}
                imdbRating={lakorn.imdbRating}
              />
            </div>
          ))
        }
      </div>
      
      <button class="carousel-nav-btn prev-btn" id="carousel-prev" aria-label="Попередній">❮</button>
      <button class="carousel-nav-btn next-btn" id="carousel-next" aria-label="Наступний">❯</button>
      
      <div class="carousel-dots" id="carousel-dots"></div>
    </div>
  </section>

  <section class="filters-section">
    <div class="filter-group">
      <label for="filter-country">Країна</label>
      <select id="filter-country">
        <option value="all">Усі країни</option>
        <option value="thailand">Таїланд</option>
        <option value="korea">Південна Корея</option>
        <option value="japan">Японія</option>
        <option value="china">Китай</option>
        <option value="taiwan">Тайвань</option>
      </select>
    </div>

    <div class="filter-group">
      <label for="filter-genre">Жанр</label>
      <select id="filter-genre">
        <option value="all">Усі жанри</option>
        <option value="romance">Романтика</option>
        <option value="drama">Драма</option>
        <option value="comedy">Комедія</option>
        <option value="crime">Кримінал</option>
        <option value="sports">Спорт</option>
        <option value="music">Музика</option>
        <option value="routine">Буденність</option>
        <option value="fantasy">Фентезі</option>
        <option value="fantastic">Фантастика</option>
        <option value="action">Бойовик</option>
        <option value="horrors">Жахи</option>
        <option value="supernatural">Надприродне</option>
        <option value="thriller">Триллер</option>
        <option value="mysticism">Містика</option>
        <option value="youth">Юність</option>
        <option value="law">Закон</option>
        <option value="melodrama">Мелодрама</option>
        <option value="business">Бізнес</option>
        <option value="food">Страви</option>
        <option value="medical">Медицина</option>
        <option value="historical">Історичне</option>
        <option value="psychological">Психологія</option>
        <option value="life">Життя</option>
      </select>
    </div>

    <div class="filter-group">
      <label for="filter-year">Рік</label>
      <select id="filter-year">
        <option value="all">Усі роки</option>
        <option value="2026">2026</option>
        <option value="2025">2025</option>
        <option value="2024">2024</option>
        <option value="2023">2023</option>
        <option value="2022">2022</option>
      </select>
    </div>
  </section>

  <section class="catalog-section">
    <h2>Усі релізи</h2>

    <div class="movies-grid catalog-fixed-grid" id="catalog-grid">
      {
        latestLakorns.map((lakorn) => (
          <div 
            class="catalog-card-wrapper" 
            data-id={lakorn.id}
            data-title={lakorn.title.toLowerCase()}
            data-en-title={lakorn.enTitle ? lakorn.enTitle.toLowerCase() : ""}
            data-status={lakorn.status.toUpperCase()}
            data-country={lakorn.country}
            data-year={lakorn.year}
            data-genres={lakorn.genres.join(",")}
          >
            <MovieCard
              id={lakorn.id}
              title={lakorn.title}
              enTitle={lakorn.enTitle}
              cover={lakorn.cover}
              status={lakorn.status}
              episodesCount={lakorn.episodesCount}
              country={lakorn.country}
              year={lakorn.year}
              genres={lakorn.genres}
              imdbRating={lakorn.imdbRating}
            />
          </div>
        ))
      }
    </div>

    <div class="pagination-container" id="pagination-controls"></div>

    <div id="hidden-full-database" style="display: none;">
      {
        lakornsData.map((lakorn) => (
          <div 
            class="database-card-source" 
            data-id={lakorn.id}
            data-title={lakorn.title.toLowerCase()}
            data-en-title={lakorn.enTitle ? lakorn.enTitle.toLowerCase() : ""}
            data-status={lakorn.status.toUpperCase()}
            data-country={lakorn.country}
            data-year={lakorn.year}
            data-genres={lakorn.genres.join(",")}
          >
            <MovieCard
              id={lakorn.id}
              title={lakorn.title}
              enTitle={lakorn.enTitle}
              cover={lakorn.cover}
              status={lakorn.status}
              episodesCount={lakorn.episodesCount}
              country={lakorn.country}
              year={lakorn.year}
              genres={lakorn.genres}
              imdbRating={lakorn.imdbRating}
            />
          </div>
        ))
      }
    </div>

    <div id="no-results" class="no-results-msg" style="display: none;">
      <p>🔍 На жаль, нічого не знайдено за такими критеріями фільтрації.</p>
    </div>
  </section>
</MainLayout>

<script>
  function initPageFeatures() {
    const catalogGrid = document.getElementById("catalog-grid");
    const dbSourceContainer = document.getElementById("hidden-full-database");
    const paginationControls = document.getElementById("pagination-controls");
    
    if (!catalogGrid || !dbSourceContainer) return;

    const ITEMS_PER_PAGE = 24; // Кількість елементів на сторінку
    let currentPage = 1;
    let currentFilteredCards: Element[] = []; // Тимчасовий масив під поточні результати

    function applyImageFix(container: HTMLElement) {
      const images = container.querySelectorAll('img');
      images.forEach(img => {
        img.style.setProperty('width', '100%', 'important');
        img.style.setProperty('height', 'auto', 'important');
        img.style.setProperty('aspect-ratio', '2 / 3', 'important');
        img.style.setProperty('object-fit', 'cover', 'important');
        img.style.setProperty('display', 'block', 'important');
      });
    }

    applyImageFix(catalogGrid);
    applyImageFix(dbSourceContainer);

    const allSourceCards = Array.from(dbSourceContainer.querySelectorAll(".database-card-source"));

    // Попередньо сортуємо початкову повноцінну базу релізів (Онгоїнги перші, потім випадкові завершені)
    const ongoingSources = allSourceCards.filter(card => card.getAttribute("data-status") === "ОНГОІНГ");
    const completedSources = allSourceCards.filter(card => card.getAttribute("data-status") !== "ОНГОІНГ");
    const shuffledCompletedSources = [...completedSources].sort(() => 0.5 - Math.random());
    const sortedSourceCards = [...ongoingSources, ...shuffledCompletedSources];

    const searchInput = document.querySelector('input[type="text"], #global-search') as HTMLInputElement;
    const countrySelect = document.getElementById("filter-country") as HTMLSelectElement;
    const genreSelect = document.getElementById("filter-genre") as HTMLSelectElement;
    const yearSelect = document.getElementById("filter-year") as HTMLSelectElement;
    const noResults = document.getElementById("no-results");

    // 🎯 ФУНКЦІЯ ВІДОБРАЖЕННЯ СТОРІНКИ З 24 КАРТКАМИ
    function displayCurrentPage() {
      if (!catalogGrid) return;
      catalogGrid.innerHTML = "";

      const startIndex = (currentPage - 1) * ITEMS_PER_PAGE;
      const endIndex = startIndex + ITEMS_PER_PAGE;
      const pageCards = currentFilteredCards.slice(startIndex, endIndex);

      pageCards.forEach((sourceCard) => {
        const clonedCard = sourceCard.cloneNode(true) as HTMLElement;
        clonedCard.className = "catalog-card-wrapper";
        catalogGrid.appendChild(clonedCard);
      });

      applyImageFix(catalogGrid);
    }

    // 🎯 ФУНКЦІЯ ГЕНЕРАЦІЇ КНОПОК ПАГІНАЦІЇ
    function renderPagination() {
      if (!paginationControls) return;
      paginationControls.innerHTML = "";

      const totalPages = Math.ceil(currentFilteredCards.length / ITEMS_PER_PAGE);
      if (totalPages <= 1) return; // Якщо сторінка лише одна — пагінацію не малюємо

      for (let i = 1; i <= totalPages; i++) {
        const button = document.createElement("button");
        button.innerText = i.toString();
        button.className = `pagination-btn ${i === currentPage ? "active" : ""}`;
        
        button.addEventListener("click", () => {
          currentPage = i;
          displayCurrentPage();
          renderPagination();
          
          // Плавно піднімаємо до заголовка каталогу
          document.querySelector('.catalog-section')?.scrollIntoView({ behavior: 'smooth' });
        });

        paginationControls.appendChild(button);
      }
    }

    // ГОЛОВНА ФУНКЦІЯ ФІЛЬТРАЦІЇ ТА ОНОВЛЕННЯ СТОРІНОК
    function filterCatalog() {
      const searchQuery = searchInput ? searchInput.value.toLowerCase().trim() : "";
      const selectedCountry = countrySelect ? countrySelect.value : "all";
      const selectedGenre = genreSelect ? genreSelect.value : "all";
      const selectedYear = yearSelect ? yearSelect.value : "all";

      // Фільтруємо сортований масив з повної бази
      currentFilteredCards = sortedSourceCards.filter((sourceCard) => {
        const titleUa = sourceCard.getAttribute("data-title") || "";
        const titleEn = sourceCard.getAttribute("data-en-title") || "";
        const country = sourceCard.getAttribute("data-country") || "";
        const year = sourceCard.getAttribute("data-year") || "";
        const genres = (sourceCard.getAttribute("data-genres") || "").split(",");

        const matchesSearch = searchQuery === "" || titleUa.includes(searchQuery) || titleEn.includes(searchQuery);
        const matchesCountry = selectedCountry === "all" || country === selectedCountry;
        const matchesYear = selectedYear === "all" || year === selectedYear;
        const matchesGenre = selectedGenre === "all" || genres.includes(selectedGenre);

        return matchesSearch && matchesCountry && matchesYear && matchesGenre;
      });

      // Відображаємо результат з першої сторінки
      displayCurrentPage();
      renderPagination();

      if (noResults) {
        noResults.style.display = currentFilteredCards.length === 0 ? "block" : "none";
      }
    }

    // Слухачі подій
    searchInput?.addEventListener("input", () => { currentPage = 1; filterCatalog(); });
    countrySelect?.addEventListener("change", () => { currentPage = 1; filterCatalog(); });
    genreSelect?.addEventListener("change", () => { currentPage = 1; filterCatalog(); });
    yearSelect?.addEventListener("change", () => { currentPage = 1; filterCatalog(); });

    // Початкова ініціалізація сторінки за замовчуванням
    filterCatalog();

    // Код каруселі
    const track = document.getElementById('carousel-track');
    const dotsContainer = document.getElementById('carousel-dots');
    const prevBtn = document.getElementById('carousel-prev');
    const nextBtn = document.getElementById('carousel-next');
    
    if (!track) return;

    const allSlides = Array.from(document.querySelectorAll('.carousel-slide-wrapper'));
    if (allSlides.length === 0) return;

    track.innerHTML = '';
    if (dotsContainer) dotsContainer.innerHTML = '';

    const shuffled = [...allSlides].sort(() => 0.5 - Math.random());
    const selectedSlides = shuffled.slice(0, 7);

    selectedSlides.forEach((slide) => {
      slide.classList.add('carousel-active');
      track.appendChild(slide);
    });

    applyImageFix(track);

    function getSlidesPerPage() {
      const width = window.innerWidth;
      if (width >= 1200) return 4;
      if (width >= 900) return 3;
      if (width >= 480) return 2;
      return 1;
    }

    let currentIndex = 0;
    let autoSlideInterval: number;

    function buildDots() {
      if (!dotsContainer) return;
      dotsContainer.innerHTML = '';
      const slidesPerPage = getSlidesPerPage();
      const maxIndex = Math.max(1, selectedSlides.length - slidesPerPage + 1);

      for (let i = 0; i < maxIndex; i++) {
        const dot = document.createElement('div');
        dot.className = `carousel-dot ${i === currentIndex ? 'active' : ''}`;
        dot.dataset.index = i.toString();
        dotsContainer.appendChild(dot);
      }
    }

    function updateSlider(index: number) {
      const slidesPerPage = getSlidesPerPage();
      const maxIndex = Math.max(1, selectedSlides.length - slidesPerPage + 1);

      if (index >= maxIndex) currentIndex = 0;
      else if (index < 0) currentIndex = maxIndex - 1;
      else currentIndex = index;

      const slideWidthPercentage = 100 / slidesPerPage;
      track!.style.transform = `translateX(-${currentIndex * slideWidthPercentage}%)`;

      const dots = document.querySelectorAll('.carousel-dot');
      dots.forEach((dot, i) => {
        dot.classList.toggle('active', i === currentIndex);
      });
    }

    function startAutoSlide() {
      stopAutoSlide();
      autoSlideInterval = window.setInterval(() => {
        updateSlider(currentIndex + 1);
      }, 5000);
    }

    function stopAutoSlide() {
      if (autoSlideInterval) clearInterval(autoSlideInterval);
    }

    nextBtn?.addEventListener('click', (e) => {
      e.preventDefault();
      updateSlider(currentIndex + 1);
      startAutoSlide();
    });

    prevBtn?.addEventListener('click', (e) => {
      e.preventDefault();
      updateSlider(currentIndex - 1);
      startAutoSlide();
    });

    dotsContainer?.addEventListener('click', (e) => {
      const target = e.target as HTMLElement;
      if (target && target.classList.contains('carousel-dot')) {
        const idx = parseInt(target.getAttribute('data-index') || '0', 10);
        updateSlider(idx);
        startAutoSlide();
      }
    });

    buildDots();
    updateSlider(currentIndex);
    startAutoSlide();

    window.addEventListener('resize', () => {
      buildDots();
      updateSlider(currentIndex);
    });
  }

  document.addEventListener("DOMContentLoaded", initPageFeatures);
  document.addEventListener("astro:after-swap", initPageFeatures);
</script>

<style is:global>
  /* 📊 СТИЛІ КНОПОК ПАГІНАЦІЇ */
  .pagination-container {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 8px;
    margin: 40px auto 10px auto;
    width: 100%;
    max-width: 1200px;
  }

  .pagination-btn {
    background-color: #0f172a;
    color: #94a3b8;
    border: 1px solid rgba(99, 102, 241, 0.2);
    padding: 10px 16px;
    border-radius: 8px;
    cursor: pointer;
    font-weight: 600;
    font-size: 14px;
    transition: all 0.25s ease;
  }

  .pagination-btn:hover {
    background-color: #1e1b4b;
    border-color: #6366f1;
    color: #fff;
  }

  .pagination-btn.active {
    background-color: #6366f1;
    color: #fff;
    border-color: #6366f1;
    box-shadow: 0 0 12px rgba(99, 102, 241, 0.4);
  }

  /* Світла тема для пагінації */
  :global([data-theme="light"]) .pagination-btn {
    background-color: #e2e8f0;
    color: #0f172a;
    border-color: rgba(15, 23, 42, 0.15);
  }
  :global([data-theme="light"]) .pagination-btn:hover {
    background-color: #cbd5e1;
  }
  :global([data-theme="light"]) .pagination-btn.active {
    background-color: #0f172a;
    color: #fff;
    border-color: #0f172a;
  }

  /* 🏛️ ЖОРСТКИЙ СЕЛЕКТОР НА 4 КАРТКИ В РЯД ДЛЯ КАТАЛОГУ */
  .catalog-fixed-grid { 
    display: grid !important; 
    grid-template-columns: repeat(4, 1fr) !important;
    gap: 25px !important; 
  }
  
  @media (max-width: 1024px) {
    .catalog-fixed-grid { 
      grid-template-columns: repeat(3, 1fr) !important; 
    }
  }

  @media (max-width: 600px) {
    .catalog-fixed-grid { 
      grid-template-columns: repeat(2, 1fr) !important; 
      gap: 12px !important; 
    }
  }

  .filters-container { display: flex; gap: 20px; width: 100%; }
  .filter-item { flex: 1; }
  @media (max-width: 768px) { .filters-container { flex-direction: column; gap: 12px; } }

  .filters-section {
    display: flex; gap: 20px; background-color: var(--bg-card) !important;
    padding: 20px; border-radius: 12px; border: 1px solid var(--border-color);
    margin-top: 30px; flex-wrap: wrap; transition: background-color 0.3s ease, border-color 0.3s ease;
  }
  [data-theme="light"] .filters-section { display: flex; gap: 20px; background-color: #7C9BCC !important; }
  .filter-group { display: flex; flex-direction: column; gap: 8px; flex: 1; min-width: 140px; }
  .filter-group label { font-size: 13px; font-weight: 700; color: var(--text-muted) !important; text-transform: uppercase; letter-spacing: 0.5px; }
  .filter-group select { background-color: var(--bg-select) !important; border: 1px solid var(--border-color) !important; color: var(--text-primary) !important; padding: 10px 14px; border-radius: 8px; font-size: 14px; font-weight: 500; cursor: pointer; outline: none; width: 100%; }
  .filter-group select:focus { border-color: #6366f1 !important; }

  .catalog-section { margin-top: 40px; }
  .catalog-section h2 { font-size: 22px; font-weight: 600; margin-bottom: 25px; border-left: 4px solid #6366f1; padding-left: 12px; color: var(--text-primary) !important; }
  
  .catalog-card-wrapper {
    display: flex;
    flex-direction: column;
    width: 100%;
  }

  .no-results-msg { text-align: center; padding: 40px; color: var(--text-muted); font-size: 16px; background-color: var(--bg-card); border-radius: 12px; border: 1px solid var(--border-color); }
  
  .hero-carousel-section {
    width: 100%;
    margin: 1.5rem auto;
    box-sizing: border-box;
    padding: 0 10px;
  }

  .carousel-container {
    position: relative;
    width: 100%;
    margin: 0 auto;
    border-radius: 16px;
    overflow: hidden;
  }

  .carousel-track {
    display: flex;
    gap: 16px;
    transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    will-change: transform;
  }

  .carousel-slide-wrapper {
    box-sizing: border-box;
    display: none;
    flex: 0 0 calc(25% - 12px); 
  }

  @media (max-width: 1200px) {
    .carousel-slide-wrapper { flex: 0 0 calc(33.333% - 11px); }
  }
  @media (max-width: 900px) {
    .carousel-slide-wrapper { flex: 0 0 calc(50% - 8px); }
  }
  @media (max-width: 480px) {
    .carousel-slide-wrapper { flex: 0 0 calc(50% - 8px); }
  }
  
  .carousel-slide-wrapper.carousel-active {
    display: block;
  }

  .carousel-slide-wrapper .movie-card {
    width: 100% !important;
    margin: 0 !important;
  }

  .carousel-nav-btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(15, 23, 42, 0.75);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: #fff;
    width: 38px;
    height: 38px;
    border-radius: 50%;
    font-size: 1rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s ease;
    backdrop-filter: blur(6px);
    z-index: 10;
  }
  @media (max-width: 600px) {
    .carousel-nav-btn { width: 32px; height: 32px; font-size: 0.8rem; }
  }
  .carousel-nav-btn:hover { background-color: #6366f1; border-color: #6366f1; transform: translateY(-50%) scale(1.05); }
  .prev-btn { left: 5px; }
  .next-btn { right: 5px; }

  .carousel-dots {
    display: flex;
    justify-content: center;
    gap: 8px;
    margin-top: 15px;
  }
  
  .carousel-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.25);
    cursor: pointer;
    transition: all 0.3s ease;
  }
  [data-theme="light"] .carousel-dot { background: rgba(0, 0, 0, 0.15); }
  
  .carousel-dot.active {
    background: #6366f1;
    width: 20px;
    border-radius: 4px;
  }

  #catalog-grid,
  .movies-grid { 
    display: grid !important; 
    grid-template-columns: repeat(4, 1fr) !important;
    gap: 24px !important; 
    width: 100% !important;
  }
  
  @media (max-width: 1024px) {
    #catalog-grid, .movies-grid { 
      grid-template-columns: repeat(3, 1fr) !important; 
    }
  }

  @media (max-width: 600px) {
    #catalog-grid, .movies-grid { 
      grid-template-columns: repeat(2, 1fr) !important; 
      gap: 12px !important; 
    }
  }
</style>