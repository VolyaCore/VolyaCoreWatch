## Як запустити
1. **Підтягуємо те, що написав інший**
    ```bash
   git pull

2. **Додаємо те, що написали**
    ```bash
   git add . 

3. **Підписуємо(в лапках міняємо)**
    ```bash
   git commit -m "опис змін"

3. **Вантажимо на гітхаб**
    ```bash
   git push
